{
  "name": "mi-obra",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
   "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:live": "nodemon server.js --watch server.js --watch views --watch public & browser-sync start --proxy http://localhost:3000 --files \"views/**/*,public/**/*\" --no-open"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-ejs-layouts": "^2.5.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "nodemon": "^3.1.7"
  }
}
